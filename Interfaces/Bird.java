package Interfaces;


// by default every interface is public or default (No protected and private access modifiers are allowed for interface)
interface Bird {
    // every method in interface is public abstract
    public abstract void fly();
}
